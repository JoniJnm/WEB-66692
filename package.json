{
  "name": "web-66692",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run build-app && npm run build-worker",
    "build-app": "npx tsc -p tsconfig.app.json",
    "build-worker": "npx tsc -p tsconfig.worker.json",
    "serve": "http-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "~5.4.5",
    "http-server": "~14.1.1"
  }
}
